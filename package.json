{
  "name": "learning-rust-nodejs",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "author": "Tony Narlock <tony@git-pull.com>",
  "license": "UNLICENSED",
  "private": true,
  "packageManager": "pnpm@10.16.0",
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "cargo-cp-artifact": "^0.1.9"
  },
  "scripts": {
    "cargo-build": "cd native && cargo-cp-artifact -a cdylib learning-rust-nodejs index.node -- cargo build --message-format=json-render-diagnostics",
    "build": "pnpm run cargo-build --release",
    "build:dev": "pnpm run cargo-build",
    "pretest": "npm run lint && cd native && cargo test",
    "test": "node lib/index.js",
    "lint": "cd native && cargo fmt -- --check && cargo clippy -- -D warnings",
    "lint:fix": "cd native && cargo fmt",
    "check": "cd native && cargo check"
  }
}
